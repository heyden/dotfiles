
# System
alias sshgh='eval $(ssh-agent -s) && ssh-add ~/.ssh/github_id_rsa'

# Git
alias gits='git status -sb'
alias gpu='git pull'
alias gp='git push'

# Node

# Go
alias go-build='go build -a ./...'
alias go-buildrace='go build -a -race ./...'
alias go-install='go install -a ./...'
